name: Build Chocofi Knob Studio

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/main.yml
    secrets: inherit
    with:
      board: '["rp2040_zero"]'
      shield: '["chocofi_knob"]'
      alphas: '["QWERTY"]'
