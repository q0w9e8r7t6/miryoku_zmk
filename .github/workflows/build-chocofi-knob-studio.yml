name: Build Pabile38 Studio

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/main.yml
    secrets: inherit
    with:
      board: '["rp2040_zero"]'
      shield: '["pabile38"]'
      alphas: '["QWERTY"]'
